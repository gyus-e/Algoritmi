class PostorderDepthIterator
{
	Grafo G
	Node curr

	Stack S
	int idx

	Vector color
}

PostorderDepthIterator ()
{
    G = grafo

	if (G.V.Size() == 0)
	{
		throw exception
	}

    InitColor ()

    curr = V[0]
    S.Push(curr, itr)
    color[curr] = gray

    idx = 1
    
    LeftMostLeaf()
    //foglia piú a sinistra nell'albero radicato in v della foresta di visita
}

Operator++()
{
	while (!S.Empty())
	{
		(curr, itr) = S.Top()
		while (!Terminated())
		{
			w = *itr; //?
			itr++; //?
			
			if (c[w] == white)
			{
				S.push (w,0)
				c[w] = gray
				
				LeftMostLeaf()
				
				return;
			}
		}
	}
}

Terminated()
{
	return idx >= G.V.Size()
}